import React from 'react';

export default function Platform(){
    return <>
        <h3>PLATFORM</h3>
        <p>The platforms are saved with grid-based positions and sizes, to allow them to be responsive very easily.</p>
        <p>Below is an example of rendering and responsive platforms using Phaser:</p>
    </>;
}